# 牛来 业务约束文档

## 1. 概述

- **名称**：牛来
- **形态**：企业微信智能机器人（API 模式，WebSocket 长连接）
- **目标部署平台**：macOS、Linux
- **核心行为**：在工作时间内随机触发“发妈妈”事件，被正确指令制止后进入冷却

## 2. 工作时间定义

工作时间由两个条件共同决定：

1. **工作日**：由 `WORK_DAYS` 环境变量指定，使用逗号分隔的数字
   - `1` = 周一，`2` = 周二，……，`7` = 周日
   - 默认：`1,2,3,4,5`（周一到周五）
2. **工作时段**：由 `WORK_START_TIME` 和 `WORK_END_TIME` 指定，格式 `HH:MM`（24 小时制）
   - 默认：`09:00` 到 `18:00`

只有同时满足“当天是工作日”且“当前时间在工作时段内”时，才可能触发事件

## 3. 事件触发规则

- 应用处于 `IDLE` 状态且当前处于工作时间时，每隔一段检查周期评估是否触发
- 触发后进入 `SCREAMING` 状态，向目标会话发送“妈妈”
- 触发策略采用概率触发：每个检查周期按一定概率进入事件，确保在工作时间内随机、不可预测地发生

## 4. 持续发送规则

- 进入 `SCREAMING` 状态后，每隔 `MIN_INTERVAL_SECONDS` 到 `MAX_INTERVAL_SECONDS` 之间的随机秒数发送一次“妈妈”
- 默认间隔：15 秒到 30 秒
- 发送内容固定为：`妈妈`
- 发送动作一直持续，直到满足停止条件

## 5. 停止条件

当收到企业微信消息回调时，若同时满足：

1. 消息类型为文本；
2. 消息文本中包含“牛来”；
3. 消息中 @ 了当前机器人（回调中体现为机器人被提及）

则立即停止发送，状态切换为 `COOLDOWN`，并回复一句确认消息（如“好的，我安静了”）

## 6. 冷却规则

- 进入 `COOLDOWN` 状态后，在 `COOLDOWN_MINUTES` 分钟内不再触发新事件
- 默认冷却时长：120 分钟（2 小时）
- 冷却期间即使仍处于工作时间，也保持 `COOLDOWN`，不触发事件
- 冷却结束后自动回到 `IDLE`

## 7. 状态机

```text
+------+  工作时间概率触发   +-----------+
| IDLE | ------------------> | SCREAMING |
+------+                     +-----------+
                                |
                                | 每 15-30s 发送“妈妈”
                                |
                                | @机器人 + 文本含“牛来”
                                v
+------+   冷却 2h 后复位    +-----------+
| IDLE | <------------------ | COOLDOWN  |
+------+                     +-----------+
```

## 8. 环境变量

| 变量 | 必填 | 默认值 | 说明 |
|---|---|---|---|
| `WECOM_BOT_ID` | 是 | - | 智能机器人 Bot ID |
| `WECOM_BOT_SECRET` | 是 | - | 智能机器人 Secret |
| `WORK_START_TIME` | 否 | `09:00` | 工作开始时间 |
| `WORK_END_TIME` | 否 | `18:00` | 工作结束时间 |
| `WORK_DAYS` | 否 | `1,2,3,4,5` | 工作星期 |
| `MIN_INTERVAL_SECONDS` | 否 | `15` | 发送间隔下限 |
| `MAX_INTERVAL_SECONDS` | 否 | `30` | 发送间隔上限 |
| `COOLDOWN_MINUTES` | 否 | `120` | 冷却时长（分钟） |
| `TARGET_CHAT_ID` | 否 | - | 主动发送目标会话 ID |
| `LOG_LEVEL` | 否 | `info` | 日志级别：debug、info、warn、error |

## 9. WebSocket 协议要点

- 连接地址：`wss://openws.work.weixin.qq.com`
- 认证帧：`aibot_subscribe`，携带 `botid` 和 `secret`
- 心跳：每 30 秒发送 `ping` 帧
- 接收事件：`aibot_msg_callback`（用户消息）、`aibot_event_callback`（事件）
- 主动发送：`aibot_send_msg`，需指定目标会话 ID 和消息内容
- 断线后必须自动重连，重连间隔采用指数退避（如 1s、2s、4s、8s……上限 60s）

## 10. 安全与部署

- Bot ID 和 Secret 必须通过环境变量注入，不得写入代码或配置文件
- 服务以单进程方式运行，状态保存在内存中
- 服务重启后状态重置为 `IDLE`
- 构建产物为单个可执行文件，方便在 macOS 和 Linux 上部署
