# 牛来 业务约束文档

## 1. 概述

- **名称**：牛来
- **形态**：企业微信智能机器人（API 模式，WebSocket 长连接）
- **目标部署平台**：macOS、Linux
- **核心行为**：在工作时间内随机触发“发妈妈”事件，被正确指令制止后进入冷却
- **会话隔离**：每个目标群聊是一个相互独立的会话，各自触发、发送、停止与冷却，互不影响

## 2. 工作时间定义

工作时间由两个条件共同决定：

1. **工作日**：由 `WORK_DAYS` 环境变量指定，使用逗号分隔的数字
   - `1` = 周一，`2` = 周二，……，`7` = 周日
   - 默认：`1,2,3,4,5`（周一到周五）
2. **工作时段**：由 `WORK_START_TIME` 和 `WORK_END_TIME` 指定，格式 `HH:MM`（24 小时制）
   - 默认：`09:00` 到 `18:00`

只有同时满足“当天是工作日”且“当前时间在工作时段内”时，才可能触发事件

## 3. 事件触发规则

- 每个目标群聊独立判定触发：该群处于 `IDLE` 状态且当前处于工作时间时，每隔一段检查周期评估是否触发；一个群的触发不影响其他群
- 触发后该群进入 `SCREAMING` 状态，向该群发送喊话内容（`SCREAM_CONTENT`，默认“妈妈”）
- 触发策略采用概率触发：每个检查周期按一定概率进入事件，确保在工作时间内随机、不可预测地发生
- **每日保底**：每个目标群聊每个工作日至少触发一次。进入工作结束前 `FORCE_TRIGGER_WINDOW_MINUTES` 分钟的窗口后（默认 30 分钟），若某群当天未成功触发，则对该群不再依赖概率，直接强制触发；已触发的群不受影响
- 保底判定以“向该群聊发送成功”为准；发送失败不计入当日已触发
- 保底仅在该群 `IDLE` 且工作时间内生效：若保底窗口内该群处于 `SCREAMING` 或 `COOLDOWN`，则不强制触发

## 4. 持续发送规则

- 某个群进入 `SCREAMING` 状态后，每隔 `MIN_INTERVAL_SECONDS` 到 `MAX_INTERVAL_SECONDS` 之间的随机秒数向该群发送一次喊话内容；各群的发送循环与间隔相互独立
- 默认间隔：15 秒到 30 秒
- 发送内容由 `SCREAM_CONTENT` 指定，默认：`妈妈`
- 发送目标：
  - 如果配置了 `TARGET_CHAT_ID`，则只向这些会话发送
  - 否则向运行时收集到的所有群聊逐个发送
- 对单个群聊连续发送失败 `MAX_SEND_FAILURES` 次后，自动将其移出目标群聊列表（默认 3 次），并终止该群的会话；该群之后被重新发现时以全新 `IDLE` 会话参与触发
- 发送动作一直持续，直到该群满足停止条件

## 5. 目标群聊发现规则

- 机器人从收到的 `aibot_msg_callback` 或 `aibot_event_callback` 中自动收集 `chattype=group` 的 `chatid`
- 自动收集到的 `chatid` 会增量合并到 `TARGET_CHAT_ID`，已有 ID 不重复添加
- 目标群聊列表每次变化（自动发现或失败移除）都会同步回写工作目录下的 `.env` 文件（文件不存在时跳过，如纯环境变量部署），重启后仍然有效
- 企业微信智能机器人目前**不支持**进群/退群事件回调，因此无法主动感知“机器人在哪些群”，只能被动发现产生过交互的群
- 被连续发送失败 `MAX_SEND_FAILURES` 次的群聊会自动从列表中移除，移除同样回写 `.env`（含手工配置的 ID）；若是网络抖动等临时故障误伤，待该群再产生消息时会被重新发现并再次加入

## 6. 停止条件

当收到企业微信消息回调时，若同时满足：

1. 消息类型为文本；
2. 消息文本按以下管道处理后包含停止关键词（`STOP_KEYWORD`，默认“牛来”，关键词中的空白会被忽略）：
   1. 循环剥离开头连续的 @提及 段：提及以 `@` 开始、以空白类字符（普通空格、全角空格、零宽空格等）为分隔符，如“@牛来 ”；无分隔符的“@xxx”无法确定边界，不在此步剥离；
   2. 去除所有 Unicode 空白字符（含零宽空格）；
   3. 剥离仍残留在开头的“@关键词”前缀，兜底无分隔符的提及（如“@牛来不要停”）与纯提及（“@牛来”）；

则立即停止该群的发送，该群状态切换为 `COOLDOWN`，并向该消息发送一条“完成回复”。停止只作用于收到指令的群聊，其他群聊的 `SCREAMING` 不受影响

注意：只有开头的提及会被剥离（@ 仅表示唤起机器人，其后内容才是真实指令）；出现在句中或句尾的提及视为普通文本，仍参与关键词匹配

## 7. 完成后回复规则

- 停止指令生效（`SCREAMING` 切换为 `COOLDOWN`）后，通过 `aibot_respond_msg` 回复触发停止的那条消息；指令未生效（未处于 `SCREAMING`）时不回复
- 回复内容由 `FINISH_REPLY_TYPE` 决定：
  - `text`（默认）：回复 `FINISH_REPLY_TEXT` 指定的文本，默认发送 emoji 🐮
  - `image`：回复 `FINISH_REPLY_IMAGE_URL` 索引的图片，地址支持 http(s) URL 或本地文件路径
- 图片回复需先经 `aibot_upload_media_init/chunk/finish` 上传临时素材换取 `media_id`；`media_id` 在进程内按地址缓存，重启后重新上传
- 图片拉取或上传失败时回退为文本回复（`FINISH_REPLY_TEXT`，默认 🐮），保证完成信号可达

## 8. 冷却规则

- 某个群进入 `COOLDOWN` 状态后，该群在 `COOLDOWN_MINUTES` 分钟内不再触发新事件；冷却按群独立计算，不影响其他群
- 默认冷却时长：120 分钟（2 小时）
- 冷却期间即使仍处于工作时间，该群也保持 `COOLDOWN`，不触发事件
- 冷却结束后该群自动回到 `IDLE`

## 9. 状态机

每个目标群聊各有一份独立的状态机，状态流转互不影响：

```text
+------+  工作时间概率触发 / 保底窗口内强制触发   +-----------+
| IDLE | --------------------------------------> | SCREAMING |
+------+                                          +-----------+
                                |
                                | 每 15-30s 向该群发送喊话内容（默认“妈妈”）
                                |
                                | 该群收到剥离开头提及后文本含停止关键词：停止并回复（默认 🐮）
                                v
+------+   冷却 2h 后复位    +-----------+
| IDLE | <------------------ | COOLDOWN  |
+------+                     +-----------+
```

## 10. 环境变量

| 变量 | 必填 | 默认值 | 说明 |
|---|---|---|---|
| `WECOM_BOT_ID` | 是 | - | 智能机器人 Bot ID |
| `WECOM_BOT_SECRET` | 是 | - | 智能机器人 Secret |
| `WORK_START_TIME` | 否 | `09:00` | 工作开始时间 |
| `WORK_END_TIME` | 否 | `18:00` | 工作结束时间 |
| `WORK_DAYS` | 否 | `1,2,3,4,5` | 工作星期 |
| `MIN_INTERVAL_SECONDS` | 否 | `15` | 发送间隔下限 |
| `MAX_INTERVAL_SECONDS` | 否 | `30` | 发送间隔上限 |
| `COOLDOWN_MINUTES` | 否 | `120` | 冷却时长（分钟） |
| `MAX_SEND_FAILURES` | 否 | `3` | 连续发送失败多少次后移除目标群聊 |
| `FORCE_TRIGGER_WINDOW_MINUTES` | 否 | `30` | 每日保底触发窗口：工作结束前该时长内若有群聊当天未触发则强制触发 |
| `SCREAM_CONTENT` | 否 | `妈妈` | 触发后向群聊发送的喊话内容 |
| `STOP_KEYWORD` | 否 | `牛来` | 停止指令关键词：文本剥离开头 @提及 后包含该词即停止；关键词中的空白会被忽略 |
| `FINISH_REPLY_TYPE` | 否 | `text` | 完成后回复类型：`text` 或 `image` |
| `FINISH_REPLY_TEXT` | 否 | `🐮` | 文本回复内容；图片回复失败时也用它回退 |
| `FINISH_REPLY_IMAGE_URL` | 类型为 `image` 时必填 | - | 图片地址索引，支持 http(s) URL 或本地文件路径 |
| `TARGET_CHAT_ID` | 否 | - | 主动发送目标会话 ID，多个 ID 用逗号分隔；运行期间自动发现的新群聊会增量加入并去重，列表变化时回写 `.env`（文件存在时） |
| `LOG_LEVEL` | 否 | `info` | 日志级别：debug、info、warn、error |

## 11. WebSocket 协议要点

- 连接地址：`wss://openws.work.weixin.qq.com`
- 认证帧：`aibot_subscribe`，携带 `botid` 和 `secret`
- 心跳：每 30 秒发送 `ping` 帧
- 接收事件：`aibot_msg_callback`（用户消息）、`aibot_event_callback`（事件）
- 主动发送：`aibot_send_msg`，需指定目标会话 ID 和消息内容
- 断线后必须自动重连，重连间隔采用指数退避（如 1s、2s、4s、8s……上限 60s）

## 12. 安全与部署

- Bot ID 和 Secret 必须通过环境变量注入，不得写入代码或配置文件
- 服务以单进程方式运行，状态保存在内存中
- 服务重启后状态重置为 `IDLE`
- 构建产物为单个可执行文件，方便在 macOS 和 Linux 上部署
